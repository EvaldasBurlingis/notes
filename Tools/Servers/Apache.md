```bash

sudo apt-get install apache2        # install apache
sudo service apache2 status         # get status
sudo service apache2 start          # start apache server
sudo service apache2 stop           # stop apache server
sudo service apache2 restart        # restart apache server

```